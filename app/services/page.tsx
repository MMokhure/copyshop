import ServicesManager from '@/components/ServicesManager';

export const metadata = { title: 'Manage Services · CopyShop' };

export default function ServicesPage() {
  return <ServicesManager />;
}
